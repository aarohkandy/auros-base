# Auros — installed to /etc/xdg/user-dirs.conf.
# Keep the folder names in step with the session language.  A Marathi recipe (check B4) should produce
# Marathi folder names, not English ones; "in their language" has to include the folders they open
# every day, not just the menus.
enabled=True
filename_encoding=UTF-8
